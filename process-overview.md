---
icon: diagram-project
---

# ภาพรวมกระบวนการทั้งหมด (Overview CMS Process)

แผนภาพนี้สรุปกระบวนการหลักทั้งหมดของระบบ CMS ตั้งแต่การเพิ่ม/ปรับปรุง/ปิดหลักสูตร ไปจนถึงการอนุมัติ

> 🟦 **สีน้ำเงิน** = ผู้ดูแลระบบ (Admin) &#x20;· 🟧 **สีเหลือง** = ผู้จัดทำหลักสูตร (เจ้าหน้าที่ระดับคณะ)

```mermaid
flowchart LR
    classDef staff fill:#F5A623,stroke:#C77C11,color:#1A1A1A;
    classDef admin fill:#1F4E79,stroke:#12395C,color:#FFFFFF;

    add["เพิ่มหลักสูตรใหม่"]:::admin
    find["ค้นหาหลักสูตรปัจจุบัน"]:::admin

    plo["ข้อมูลทั่วไป + ข้อมูล PLOs"]:::admin
    ploOK["รับรอง / อนุมัติ PLOs"]:::admin

    detail["กรอกรายละเอียดหลักสูตร 8 หมวด<br/>(ทั่วไป · รูปแบบ · ผลลัพธ์การเรียนรู้ · โครงสร้าง ·<br/>รายวิชา · ประกันคุณภาพ · บริหารจัดการ · อื่นๆ)"]:::staff

    approve["บันทึกวันที่อนุมัติ + แนบไฟล์หลักฐาน<br/>(8 ลำดับการอนุมัติ)"]:::admin
    council["อนุมัติจากสภามหาวิทยาลัย<br/>(ลำดับ 5 → หลักสูตรปีหน้าปกปัจจุบัน)"]:::admin
    allc["ข้อมูลหลักสูตรทั้งหมด"]:::admin

    minorSrc["โครงสร้าง / อาจารย์ / รายวิชา"]:::staff
    minorEdit["แก้ไขข้อมูล"]:::staff
    minorOK["อนุมัติ"]:::admin

    closeSrc["ภาคการศึกษา / ปีการศึกษา / เหตุผล"]:::staff
    closeEdit["แก้ไขข้อมูลปิดหลักสูตร"]:::staff
    closeOK["อนุมัติ"]:::admin
    suspend["ระงับรับเข้าศึกษา"]:::admin
    closed["ปิดดำเนินการ"]:::admin

    %% เส้นทางหลัก: เพิ่ม / ปรับปรุง
    add -->|กรอกข้อมูล| plo
    find -->|ปรับปรุง| plo
    plo -->|รับรอง PLOs| ploOK
    ploOK -->|อนุมัติ PLOs| detail
    detail -->|ส่งอนุมัติ| approve
    approve --> council
    council --> allc

    %% ปรับปรุงเล็กน้อย
    find -->|ปรับปรุงเล็กน้อย| minorSrc
    minorSrc --> minorEdit
    minorEdit -->|ส่งอนุมัติ| minorOK
    minorOK -->|อนุมัติจากสภาฯ| allc

    %% ปิดหลักสูตร
    find -->|ปิดหลักสูตร| closeSrc
    closeSrc --> closeEdit
    closeEdit -->|ส่งอนุมัติ| closeOK
    closeOK -->|"ไม่มี นศ. ค้าง"| closed
    closeOK -->|"มี นศ. ค้าง"| suspend
    suspend --> closed
    closed -->|อนุมัติจากสภาฯ| allc
```

## อ่านแผนภาพนี้อย่างไร

* **Admin (น้ำเงิน)** เริ่มด้วย "เพิ่มหลักสูตรใหม่" หรือ "ค้นหาหลักสูตรปัจจุบัน" (เพื่อปรับปรุง) → กรอกข้อมูล PLOs และรับรอง/อนุมัติ PLOs
* **ผู้จัดทำ (เหลือง)** กรอก **รายละเอียดหลักสูตร 8 หมวด** ให้ครบ แล้ว **ส่งอนุมัติ**
* **Admin** บันทึกวันที่อนุมัติ + แนบไฟล์ในแต่ละลำดับ จนถึง **สภามหาวิทยาลัย (ลำดับ 5)** สถานะเปลี่ยนเป็น "หลักสูตรปีหน้าปกปัจจุบัน" อัตโนมัติ แล้วเข้าสู่ "ข้อมูลหลักสูตรทั้งหมด"
* **ปรับปรุงเล็กน้อย** และ **ปิดหลักสูตร** เป็นเส้นทางแยก โดยผู้จัดทำแก้ไข/กรอกข้อมูล → ส่งอนุมัติ → Admin อนุมัติ (การปิดหลักสูตร: ถ้ายังมีนักศึกษาค้าง จะเข้าสู่ "ระงับรับเข้าศึกษา" ก่อน แล้วจึง "ปิดดำเนินการ")
